# PyArchInit dependencies
# Use >= (not ==) to support QGIS Python 3.9 through 3.12+
# CRITICAL: numpy must be exactly 1.26.4 for QGIS compatibility (numpy 2.x breaks QGIS)
#numpy==1.26.4

# Core DB packages
SQLAlchemy>=2.0.0
SQLAlchemy-Utils>=0.41.0
GeoAlchemy2>=0.14.0

# Reports & data
reportlab>=4.0.0
graphviz>=0.20
XlsxWriter>=3.1.0
openpyxl>=3.1.0
matplotlib>=3.7.0
pandas>=2.0.0
requests>=2.33.0  # security: CVE-2024-47081 (.netrc leak), CVE-2024-35195 (temp file reuse)

# Document & media
PyMuPDF>=1.26.7  # security: path traversal in _main_.py
python-docx>=1.0.0
opencv-python>=4.8.0  # Optional - may already be installed or cause issues on Windows
qrcode>=7.4

# Utilities
psutil>=5.9.0
nltk>=3.9.4  # security: zip slip (CVE-2026-0846), arbitrary file read, remote shutdown, XSS
beautifulsoup4>=4.12.0

# AI / LLM
openai>=1.0.0
anthropic>=0.20.0
# LangChain ecosystem - bumped to 1.x line for security patches
# langchain-core 1.2.22 closes: serialization injection, path traversal, template injection, f-string, SSRF
# langchain-text-splitters 1.1.2 closes: XXE, SSRF redirect
# langchain-openai 1.1.14 closes: SSRF token counting bypass
# langchain-community 0.4 latest stable line; 0.3.27 also acceptable for CVE coverage
# langsmith 0.7.31 closes: streaming token redaction bypass
langchain>=1.2.0
langchain-community>=0.4.0
langchain-core>=1.2.22
langchain-openai>=1.1.14
langchain-anthropic>=1.0.0
langchain-text-splitters>=1.1.2
langsmith>=0.7.31

# 3D visualization
pygame>=2.5.0
pyvista>=0.43.0
pyvistaqt>=0.11.0
vtk>=9.2.0

# Vector search
faiss-cpu>=1.7.4

# Graph
s3dgraphy>=0.1.20

# Remote Storage Backends (optional - install only if using remote storage)
cloudinary>=1.36.0
google-auth>=2.22.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.90.0
dropbox>=11.36.0
boto3>=1.28.0
webdavclient3>=3.14.0

# R Integration for Archeozoology (optional - requires R installed)
#pyper>=1.1.2

# Note: The following packages are installed in a virtual environment for Pottery Tools
# They are NOT installed in QGIS Python to avoid conflicts
# ultralytics>=8.2.0
# torch>=2.2.2
# torchvision>=0.17.2
# diffusers
# transformers
# peft
# scikit-image

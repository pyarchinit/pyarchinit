# modules/gna/gna_labels.py

## Overview

This file contains 3 documented elements.

## Functions

### get_label(key, language)

Get a translated label.

Args:
    key: Label key from GNA_LABELS
    language: Language code (it, en, de, es, fr, ar, ca)

Returns:
    Translated string or key if not found

**Parameters:**
- `key`
- `language`

### get_all_labels(language)

Get all labels for a language.

Args:
    language: Language code

Returns:
    dict with all labels in the specified language

**Parameters:**
- `language`


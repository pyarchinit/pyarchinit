---
title: "StratiGraph WP5 / T5.4 — PyArchInit ↔ s3Dgraphy Integration"
subtitle: "Development Log — Phase 1 (Foundation)"
author: "Enzo Cocca (Adarte, T5.4 Deputy Lead)"
date: "Last updated: 6 May 2026"
---

# About this document

This is the rolling development log for Task 5.4 of WP5 — PyArchInit ↔ s3Dgraphy
integration. It is updated continuously as work progresses on the
`Stratigraph_00001` branch of the PyArchInit plugin and is intended to keep
partner reviewers in sync between formal milestones (meeting minutes, Reference
Document revisions). Each entry corresponds to a logical work unit ("Group" in
the implementation plan) and lists what was built, what was tested, the
commits, and any decisions taken.

The authoritative documents remain:

- T5.4 Reference Document v0.1 (Emanuel Demetrescu, 6 May 2026) — high-level
  contract for the integration.
- Implementation plan (`docs/superpowers/plans/2026-05-06-phase-1-foundation.md`) —
  the technical task breakdown executed here.
- Spec (`docs/superpowers/specs/2026-05-04-s3dgraphy-bidirectional-sync-design.md`) —
  the design proposal aligned to both the meeting minutes and the Reference
  Document.

This log is regenerated to DOCX (`T5.4_PyArchInit_Dev_Log.docx`) whenever a new
Group is completed, so the latest version can be uploaded to Teams.

---

# Phase 1 (Foundation) — overview

Phase 1 closes action items AI01–AI05 from the T5.4 meeting of 4 May 2026. The
six deliverables are:

1. Vendor s3Dgraphy 0.1.30 → 0.1.40 inside `ext_libs/`.
2. New `VocabProvider` that parses the three s3Dgraphy JSON pillars directly
   (Option B per Reference Document v0.1 §4.5).
3. Refactor `pyarchinit_i18n_stratigraphic.py` as a backwards-compatible
   adapter over `VocabProvider`.
4. One-shot vocabulary alignment migration: `USVA/USVB → USVs`,
   `USVC → USVn`.
5. UUID v7 backfill on the three primary tables (`us_table`,
   `inventario_materiali_table`, `periodizzazione_table`) using a `node_uuid
   TEXT` column with a partial unique index.
6. Refactor 12 files under `modules/s3dgraphy/` so they consult `VocabProvider`
   instead of hardcoded EM 1.4 tables.

Plan structure: 8 Groups (0 + A through G), 37 tasks, ~150 steps,
≈ 30 commits expected. The release that closes Phase 1 is tagged
`phase1-foundation-5.1.0-alpha`. A rollback tag `pre-s3dgraphy-040` was
created at the start of execution; everything is recoverable.

| Group | Title | Status |
|---|---|---|
| 0 | Pre-flight & rollback safety | **Done** (2026-05-06) |
| A | Vendor s3Dgraphy 0.1.30 → 0.1.40 | **Done** (2026-05-06) |
| B | VocabProvider (Option B per-tool JSON parsing) | **Done** (2026-05-07) |
| C | i18n adapter refactor | **Done** (2026-05-07) |
| D | USVA/USVB → USVs, USVC → USVn migration | **Done** (2026-05-07) |
| E | `node_uuid TEXT` column + UUID v7 backfill | **Done** (2026-05-07) |
| F | 12-file `modules/s3dgraphy/` refactor | **Done** (2026-05-07) |
| G | Release packaging (5.1.0-alpha) | **Done** (2026-05-07) — tag `phase1-foundation-5.1.0-alpha` pushed |

---

# Group 0 — Pre-flight & rollback safety

**Date:** 6 May 2026 · **Status:** Done · **Commits:** `52b6f865`, `3ec3fb0a`

## What was built

- A git tag `pre-s3dgraphy-040` was created at HEAD `cf11f02` as the rollback
  point for the entire Phase 1. The tag is local-only at the moment; it will be
  pushed to `origin` together with the release tag in the final Group G step.
- An inventory of the legacy s3Dgraphy usage was captured under
  `.scratch/s3dgraphy-inventory.txt` (gitignored). It records every
  string-tagged `node.node_type = '<literal>'` assignment and every reference to
  the hard-coded `CRM_CLASSES` dictionary in `modules/s3dgraphy/`. The plan had
  estimated three string-literal assignments in `s3dgraphy_integration.py`; the
  actual count is one (line 113), with two further non-literal assignments
  (line 194 uses `.get(...)`) — Group F will reconcile against this evidence.
- A pytest scaffold for the upcoming `VocabProvider` test suite was created
  under `tests/sync/`. It includes a `conftest.py` exposing five fixtures and
  three sample JSON payloads modelled on the canonical s3Dgraphy filenames
  (`s3Dgraphy_node_datamodel.json`, `s3Dgraphy_connections_datamodel.json`,
  `em_visual_rules.json`) — all with no trailing space, matching the 0.1.40
  release shape.
- The repository's `.gitignore` was tightened from `/tests/` to `/tests/*` plus
  a re-include `!/tests/sync/`, so newly added test files in `tests/sync/` are
  tracked while legacy already-tracked test files in the rest of `tests/` are
  unaffected.

## What was tested

- All three JSON fixtures parse cleanly with `json.load`.
- `python3 -m pytest tests/sync/ --collect-only -q` exits cleanly with
  "no tests collected" (because no test files exist yet — they arrive in
  Group B).
- Spec compliance review (independent subagent, no shared context with the
  implementer): all nine verification checks pass; one observation about plan
  accuracy on the inventory count (non-blocking).

## Decisions taken

- The `vocab_dir` pytest fixture writes the sample JSONs into `tmp_path/JSON_config/`
  using the canonical filenames. This means downstream `VocabProvider` tests
  exercise the post-typo path; a separate test in Group B covers the legacy
  trailing-space filename of 0.1.30 explicitly so the migration scenario stays
  pinned.
- The rollback tag is annotated (not lightweight) so its message survives the
  final push; it carries the explicit sentence: *"Rollback point before
  s3Dgraphy 0.1.30→0.1.40 vendor swap and Phase 1 refactor."*

---

# Group A — Vendor s3Dgraphy 0.1.30 → 0.1.40

**Date:** 6 May 2026 · **Status:** Done · **Commit:** `5988ed4a`

## What was built

- The vendored s3Dgraphy was upgraded in place: `ext_libs/s3dgraphy-0.1.30.dist-info`
  removed, `pip install --target ext_libs/ --no-deps s3dgraphy==0.1.40`
  installed the new 194-file payload (3.3 MB) cleanly with no transitive
  dependencies. The new `ext_libs/s3dgraphy/__init__.py` reports
  `__version__ = "0.1.40"` and `__datamodel_version__ = "1.5.4"`.
- `requirements.txt` floor for s3Dgraphy bumped from `>=0.1.20` to `>=0.1.40`,
  which is the source-controlled record of the vendor swap. End-users who run
  `scripts/modules_installer.py` get 0.1.40 from this commit forward.
- The bundled `JSON_config/` now contains the canonical filenames
  (`s3Dgraphy_node_datamodel.json` with no trailing space — the 0.1.30 typo is
  fixed in 0.1.40). Other files present:
  `s3Dgraphy_connections_datamodel.json`, `em_visual_rules.json`,
  `em_palette_icons.json`, `em_qualia_types.json` (+ `_additions`),
  `em_extractor_types.json`, `em_document_types.json`, `new_qualia_template.json`,
  and two outdated `.ttl` files preserved by upstream for reference.
- New 0.1.40 modules confirmed present (consumed by later Groups):
  `merge/graph_merger.py` (`GraphMerger`), `exporter/graphml/graphml_patcher.py`
  (`GraphMLPatcher`), `nodes/stratigraphic_node.py` (with
  `NegativeStratigraphicUnit` and `WorkingUnit`).

## What was tested

- s3Dgraphy package imports cleanly from `ext_libs/`:
  `python3 -c "import sys; sys.path.insert(0, 'ext_libs'); import s3dgraphy;
  print(s3dgraphy.__version__)"` → `0.1.40`.
- All s3Dgraphy 0.1.40 core modules byte-compile via `py_compile`.
- Manual QGIS smoke test (US form, Harris matrix, log inspection) is deferred
  to Group G.4 — the gate that must pass before tagging the
  `phase1-foundation-5.1.0-alpha` release.

## Decisions taken

- **`ext_libs/` stays gitignored** (line 43 of `.gitignore`). PyArchInit
  follows a *runtime install* policy — the plugin's `scripts/modules_installer.py`
  pip-installs ext_libs at first launch, and `requirements.txt` is the
  authoritative version pin. The original plan called for `git add ext_libs/`
  but that would have changed long-standing repo policy and added 3.3 MB of
  vendored bytes to the repository. Group A's actual git artefact is the
  one-line `requirements.txt` bump; the 0.1.40 vendor lives only on each
  developer/user's local install. The plan was updated mid-execution to
  document this correction (the `pre-s3dgraphy-040` tag still marks the
  pre-Phase-1 commit hash but does NOT snapshot the ext_libs bytes).
- **Rollback procedure now reads:** `pip install s3dgraphy==0.1.30 --target
  ext_libs/ --no-deps`. Combined with `git checkout pre-s3dgraphy-040` for
  source-controlled state.

---

# Group B — VocabProvider (Option B per-tool JSON parsing)

**Date:** 6–7 May 2026 · **Status:** Done · **Commits:** 11 (`02d59b07` → `88e70498`)

## What was built

The `modules/s3dgraphy/sync/` package was created from scratch. It is the
PyArchInit-side adapter that consumes s3Dgraphy's three JSON pillars
directly, per the Option B preference declared in Reference Document v0.1
§4.5 (per-tool parsing rather than wrapping a typed s3Dgraphy Python API).
Every consuming surface in PyArchInit — combo boxes, validation hints,
visual rendering — will read from this single provider in subsequent
Groups; the pure-Python core can be unit-tested without QGIS, while the
Qt-aware wrapper integrates with the QGIS event loop only when needed.

The package layout:

| File | Responsibility |
|---|---|
| `modules/s3dgraphy/sync/__init__.py` | Public API surface; re-exports the typed dataclasses and the pure-Python core; provides a lazy accessor `get_vocab_provider()` for the Qt wrapper so non-Qt callers (tests, CLI scripts) never pull in PyQt at import time. |
| `modules/s3dgraphy/sync/vocab_types.py` | Five frozen dataclasses (`UnitType`, `EdgeType`, `VisualRule`, `ParadataType`, `VocabularyVersion`) plus a `Family(str, Enum)` whose values mirror the JSON top-level keys (`stratigraphic_nodes`, `paradata_nodes`, `group_nodes`, …). |
| `modules/s3dgraphy/sync/vocab_provider_core.py` | `VocabProviderCore` — pure-Python loader/queryer. Reads the three pillars from `bundled_dir`, optionally merges overrides from `overrides_dir` per top-level key, and exposes the query API: `get_unit_types(family=...)`, `get_edge_types()`, `get_legal_targets_for(...)`, `get_paradata_types()`, `get_visual_rule(...)`, `get_cidoc_mapping(...)`, `versions` property, optional `min_versions={...}` enforcement at construction time. Accepts the legacy 0.1.30 trailing-space filename `s3Dgraphy_node_datamodel .json` as a fallback. |
| `modules/s3dgraphy/sync/vocab_provider.py` | `VocabProvider` — thin Qt wrapper. Subclasses `QObject` when QGIS is importable, falls back to plain `object` otherwise. Adds a `vocabulary_changed` `pyqtSignal` and a `QFileSystemWatcher` covering both the bundled JSON directory and the user override directory, so combo-box dialogs can subscribe to changes and refresh on the fly. Defaults the bundled directory to `<plugin>/ext_libs/s3dgraphy/JSON_config/` and the overrides directory to `~/.config/pyarchinit/vocab_overrides/`. |

## What was tested

The TDD discipline laid out in plan §B was followed strictly. Every method
on `VocabProviderCore` was preceded by a failing test, then implemented,
then verified green before commit. The final test count under
`tests/sync/`:

- **17 fixture-driven tests** in `test_vocab_types.py` and
  `test_vocab_provider_core.py` covering typed dataclass shape, JSON
  loading, family filtering, edge-type loading, paradata loading, visual
  rules, CIDOC lookup, version exposure, minimum-version gating (pass +
  raise), legacy trailing-space filename fallback, and override merge
  semantics (a partial paradata override must not erase bundled
  stratigraphic types).
- **2 smoke tests** in `test_vocab_provider_smoke.py` running against the
  real `ext_libs/s3dgraphy/JSON_config/` 0.1.40 vendor installed in
  Group A. The smoke tests confirm `US`, `USVs`, `USVn` appear in the
  stratigraphic family on the actual vendored datamodel and that the
  per-pillar version field is populated. Auto-skipped when `ext_libs/`
  is missing (fresh clone before `modules_installer.py` runs).

Total: **19 passed in 0.20 s**, run via `pytest tests/sync/ -v` from the
plugin root. The runner is configured by a minimal `pyproject.toml`
([`tool.pytest.ini_options`] with `testpaths = ["tests/sync"]` and
`--import-mode=importlib --confcutdir=tests/sync`) so the new suite can
run from the CLI without QGIS bootstrap — needed because the plugin's
top-level `__init__.py` imports `qgis.PyQt.*` at module load.

Two-stage review was applied to the foundation pair (Tasks B.1 + B.2): an
independent spec-compliance reviewer confirmed byte-level match to the
plan literal source, and a code-quality reviewer approved with minor
follow-ups, all four of which were folded into the next subagent batch
without requiring a separate fix commit.

## Decisions taken

- **Pure core / Qt wrapper split** is locked in. The pure-Python
  `VocabProviderCore` is the only surface tests exercise; the Qt
  `VocabProvider` is a thin singleton-with-signal that any QGIS dialog
  can subscribe to without inheriting test infrastructure. This keeps
  the test suite QGIS-free and runnable in CI / from a bare shell.
- **`pyproject.toml` is the canonical pytest config location** for this
  plugin, scoped narrowly to `tests/sync`. Legacy tests under
  `tests/test_*.py` already required QGIS to run, so no change in
  behaviour for them; the new suite is additive.
- **The 0.1.30 trailing-space filename** (`s3Dgraphy_node_datamodel .json`
  with a literal space before the extension) is preserved as a fallback
  in `_NODE_DATAMODEL_NAMES`. It will only be removed when 0.1.30
  support is officially dropped — annotated inline so future
  maintainers find the answer without archaeology.
- **Override priority is per top-level key, not whole-file**, locked in
  by a regression test that builds a partial paradata-only override and
  asserts the bundled stratigraphic types still surface from
  `get_unit_types()`. A datacenter pulling a partial vocabulary update
  will not silently erase locally available types.
- **Public API frozen for Phase 1**: `VocabProviderCore`, `UnitType`,
  `EdgeType`, `VisualRule`, `ParadataType`, `VocabularyVersion`,
  `Family`, `get_vocab_provider()`. `SyncEngine`, `GraphProjector`,
  `ParadataStore`, `ConflictResolver` are deliberately absent — they
  belong to Phases 2 and 3.

## Plan corrections folded in mid-execution

- **`pyproject.toml` was not in the plan** but is required so the new
  test suite can run from a bare CLI. The deviation was approved during
  spec review of B.1 + B.2; the file is 6 lines, single-section, and
  scoped to `tests/sync` so legacy tests are unaffected.
- **`EdgeType.allowed_pairs` annotation tightened** from raw `tuple` to
  `tuple[tuple[str, str], ...]` (review follow-up), preserving the
  hashability contract of frozen dataclasses.

---

# Group C — i18n adapter refactor

**Date:** 7 May 2026 · **Status:** Done · **Commits:** `69dd3ac9`, `81f71a65`

## What was built

`modules/utility/pyarchinit_i18n_stratigraphic.py`, the long-standing
module that ten different files in the plugin import for unit-type
abbreviations and stratigraphic helpers, was refactored into a
**deprecation shim** over `VocabProvider` while preserving its public
surface byte-for-byte.

The behavioural change is at one place: the hard-coded
`_COMMON_ITEMS = ('USVA','USVB','USVC','USD','CON','VSF','SF','SUS',
'Combinar','Extractor','DOC','property')` tuple is replaced by a lazy
`tuple` subclass (`_CommonItemsProxy`) that resolves on first access by
calling `VocabProvider.get_unit_types()`, filtering out the per-language
US/USM equivalents (which `get_unit_type_items()` adds explicitly), and
appending the legacy non-stratigraphic UI items that are not in the
s3Dgraphy JSON datamodel (`CON`, `SUS`, `Combinar`, `Extractor`, `DOC`,
`property`). When `ext_libs/s3dgraphy/` is missing — for example on a
fresh clone before `modules_installer.py` has run — the proxy falls
back to the legacy 12-item tuple so importers never break.

`UNIT_TYPE_ABBREV` (the per-language US/USM mapping) stays hard-coded:
those abbreviations are pyarchinit-specific UI conventions that are
**not** part of the s3Dgraphy formalism and have no representation in
the JSON datamodel. `_UNIT_TYPE_LABELS`, `STRATIGRAPHIC_RELATIONS` and
the rest of the file below `is_any_unit_prefix` are untouched.

A one-time deprecation log line (via `_warn_deprecation_once()`) fires
the first time the module is imported in a session, pointing new
callers at `modules.s3dgraphy.sync.VocabProvider`. Removal of the
legacy fallback is scheduled for the 6.0.0 release.

## What was tested

- **5 backwards-compatibility regression tests** were added in
  `tests/sync/test_i18n_compat.py` *before* the refactor and run green
  against the legacy implementation, then again green against the new
  adapter — locking the public surface for at least one release cycle.
  Imports verified: `_COMMON_ITEMS`, `UNIT_TYPE_ABBREV`,
  `get_unit_type_items`, `is_us_type`, `ALL_US_ABBREVS`.
- Total `tests/sync/` test count: **24 passed in 0.31 s**.
- A controller-level smoke confirmed the end-to-end behaviour change:
  the unit-type picker dialog (Italian locale) now exposes **27 items**
  (`US, USM, USVs, USVn, SF, VSF, serSU, serUSD, serUSVn, serUSVs, USD,
  TSU, UL, BR, USN, PROP, DOC, EXT, COMB, ANG, PNG, TBNG, CON, SUS,
  Combinar, Extractor, property`) instead of the previous 14, including
  the EM 1.5 types `USVs`, `USVn`, `serSU`, `WorkingUnit (UL)`,
  `NegativeStratigraphicUnit (USN)` and the EM 1.6-bound additions that
  travelled with the 0.1.40 vendor swap. This is the observable value
  delivery of action item AI02 — the unit-type list is now driven by
  s3Dgraphy's JSON datamodel and will pick up future EM revisions
  without code changes in pyarchinit.

## Decisions taken

- **`_COMMON_ITEMS` is now a `tuple` subclass, not a plain tuple.**
  `_CommonItemsProxy(tuple)` keeps `isinstance(_COMMON_ITEMS, tuple) is
  True`, supports `+`/`in`/iteration/indexing/equality, and resolves
  lazily on first access. No consumer pickles `_COMMON_ITEMS`, so the
  proxy does not implement `__reduce__`; if a future caller serialises
  it, the resolution can be forced beforehand.
- **The 6 legacy non-stratigraphic items** (`CON`, `SUS`, `Combinar`,
  `Extractor`, `DOC`, `property`) are preserved at the tail of the
  resolved tuple. They are pyarchinit-specific UI categories that are
  not s3Dgraphy node types per Reference Document v0.1 §4.2 — keeping
  them on the pyarchinit side rather than pushing them upstream as
  JSON entries. If they later need to participate in the property
  graph, that becomes a JSON pull request against
  `s3Dgraphy_node_datamodel.json`.
- **`USVA`, `USVB`, `USVC` no longer appear in the picker** as of this
  refactor. Existing database rows that still carry those values keep
  rendering correctly (the `unita_tipo` column is free-text), but new
  rows will use the canonical `USVs` and `USVn`. Group D ships the
  one-shot migration that rewrites historical rows; until that
  migration runs, mixed-vocabulary databases are supported by both
  layers.

## Downstream consumers (informational)

Ten files import `pyarchinit_i18n_stratigraphic`. The highest-traffic
ones are `tabs/US_USM.py` (the main US/USM controller, where the
picker dialog lives), `modules/db/pyarchinit_db_manager.py`,
`modules/utility/pyarchinit_exp_USsheet_pdf.py`,
`modules/utility/pyarchinit_matrix_exp.py`, and
`tabs/Interactive_matrix.py`. None were modified by this refactor —
the public surface stayed the same. They will start seeing the
expanded vocabulary the next time the plugin reloads.

---

# Group D — USVA/USVB → USVs, USVC → USVn one-shot migration

**Date:** 7 May 2026 · **Status:** Done · **Commits:** `b8a1f227`, `15efd4da`, `b9a10b04`

## What was built

A one-shot SQL migration tool that aligns historical PyArchInit databases
to the canonical EM 1.5 vocabulary now exposed through `VocabProvider`.
Implementation is split across a shared scaffold and a migration-specific
library/CLI pair so subsequent migrations (Group E and beyond) can reuse
the boilerplate.

| File | Responsibility |
|---|---|
| `scripts/migrations/_common.py` | Shared CLI scaffold: `parse_argv()` providing the standard `--dry-run / --apply / --rollback BACKUP_PATH` action group with required `--db` argument, and `auto_backup_sqlite(db, tag)` which copies the SQLite file to `<db>.pre_<tag>_<UTC>` via `shutil.copy2`. |
| `scripts/migrations/_2026_05_us_vocabulary_alignment_lib.py` | Pure-logic library exposing the `REPLACEMENTS` mapping (`USVA → USVs`, `USVB → USVs`, `USVC → USVn`), `plan_changes(db)` for the read-only count report, and `apply_changes(db)` for the in-place rewrite. Library/CLI split lets tests exercise the logic without spawning a subprocess. |
| `scripts/migrations/2026_05_us_vocabulary_alignment.py` | CLI front-end. On `--apply`, calls `auto_backup_sqlite` first, then `apply_changes`. On `--rollback BACKUP_PATH`, restores the named backup via `shutil.copy2`. |
| `pyarchinitPlugin.py` (modified) | New `_init_migrations_menu()` and `_run_vocab_alignment_migration()` methods wired into `_init_stratigraph_sync()`, called from all 4 language branches of `initGui()`. The user-facing flow: file picker → `plan_changes` → `QMessageBox.question` confirm → `auto_backup_sqlite` + `apply_changes` → result dialog. Idempotent via a `_migrations_menu_wired` guard. Imports use the `qgis.PyQt.QtWidgets` abstraction so the same code runs on Qt5 and Qt6. |

## What was tested

- **3 unit tests** in `tests/migrations/test_common.py` cover the scaffold:
  `auto_backup_sqlite` copies the file and preserves data; `parse_argv`
  accepts each of the three actions; bare `--db` exits with `SystemExit`.
- **5 unit tests** in `tests/migrations/test_us_vocabulary_alignment.py`
  cover the migration: `plan_changes` reports correct counts on a 5-row
  fixture without mutating, `apply_changes` rewrites correctly, the
  second `--apply` is a no-op (idempotency), and the `REPLACEMENTS`
  mapping is locked in.
- **End-to-end dry-run on the real Volterra database**
  (`pyarchinitcs_SCHEDE2024.sqlite`): zero rows have `USVA`/`USVB`/`USVC`
  in `unita_tipo`, so the migration will be a no-op there. The CLI
  exits cleanly without touching the file. Recorded as a safety check
  before the manual smoke gate at G.4.
- **Combined sync + migrations test run**: `pytest tests/sync/
  tests/migrations/ -q` → **32 passed, 1 warning** (the warning is a
  `datetime.utcnow()` deprecation harmless on Python 3.13; deferred for
  cleanup when minimum Python bumps).

## Decisions taken

- **Library/CLI split for every migration.** `_2026_05_us_vocabulary_alignment_lib.py`
  contains the pure logic; `2026_05_us_vocabulary_alignment.py` is the
  thin CLI wrapper. This pattern lets `tests/migrations/` exercise the
  logic directly without `subprocess.run`, and lets the QGIS menu
  handler import the library functions directly without spawning a
  child process. Group E will follow the same split.
- **Idempotency by construction.** `apply_changes` issues `UPDATE
  us_table SET unita_tipo = ? WHERE unita_tipo = ?` for each
  `REPLACEMENTS` pair; the second run finds zero matching rows and
  returns rowcount 0 per source. No "has-already-run" sentinel column
  needed.
- **Auto-backup names live next to the database file.** The naming
  pattern `<db>.pre_<tag>_<YYYYmmddTHHMMSSZ>` keeps backups
  side-by-side with their source DB so users immediately see them in a
  file-picker. `--rollback` takes the explicit backup path so the user
  is in control of which backup gets restored.
- **`.gitignore` whitelist extended.** The same pattern that Group 0
  applied to `tests/sync/` (re-include the new test directory inside
  the existing `/tests/*` blanket ignore) was applied to
  `tests/migrations/`. Both are tracked; legacy `tests/test_*.py`
  files unaffected.
- **`pyarchinitPlugin.py` is the canonical filename** — lowercase `p` —
  matches the existing repository state, not the camelCase variant
  referenced in some older notes. The new menu wiring lives there.

## Operational notes

- **Volterra database (`pyarchinitcs_SCHEDE2024.sqlite`) needs no
  alignment.** The dry-run reports 0 rows in every replacement bucket.
  The migration is therefore a no-op when first applied to it; if the
  user later imports rows from a legacy export that used `USVA`/`USVB`/
  `USVC`, re-running the migration cleans them up.
- **Other historical PyArchInit databases** (e.g. `pyarchinit_db.sqlite`,
  the development scratchpad) should each be dry-run before `--apply`
  to know what will change. The QGIS menu does this automatically: the
  confirmation dialog shows the per-source count before mutating.

---

# Group E — `node_uuid TEXT` column + UUID v7 backfill migration

**Date:** 7 May 2026 · **Status:** Done · **Commits:** `b63927ae`, `0b93b3aa`, `602d7220`

## What was built

The second one-shot SQL migration of Phase 1: every record in the three
primary stratigraphic tables gets a stable opaque identifier so that, in
later phases, the s3Dgraphy bridge can use it as the `EMid` foreign key
on the property-graph side. Per Reference Document v0.1 §3 *"Identity is
already aligned"*, the PyArchInit per-record UID and the s3Dgraphy `EMid`
will be the same value with two names; this migration creates the value.

| File | Responsibility |
|---|---|
| `modules/s3dgraphy/sync/uuid7.py` | Local UUID v7 generator (RFC 9562 §5.7). Monotonic per-process via a threading lock; when two calls fall within the same millisecond, the second increments the random portion to keep ordering. Will be replaced with `uuid.uuid7()` once the project minimum bumps to Python 3.14+. |
| `scripts/migrations/_2026_05_node_uuid_backfill_lib.py` | Pure-logic library. `TABLES` lists the three target tables (`us_table`, `inventario_materiali_table`, `periodizzazione_table`). `add_columns(db)` adds `node_uuid TEXT` if missing and creates a partial unique index `WHERE node_uuid IS NOT NULL` on each table — the partial predicate lets multiple NULL rows coexist during a partial-failure recovery. `backfill_uuids(db)` assigns `uuid7()` to every NULL `node_uuid`; returns a per-table count of rows updated. |
| `scripts/migrations/2026_05_node_uuid_backfill.py` | CLI front-end. `--dry-run` reports either "needs ALTER TABLE + backfill (no col)" or "N row(s) need backfill" per table without mutating. `--apply` runs `auto_backup_sqlite` + `add_columns` + `backfill_uuids`. `--rollback` restores an explicit backup path. |
| `pyarchinitPlugin.py` (modified) | Second entry in `_init_migrations_menu()`: file picker → confirmation dialog showing the three target tables → `auto_backup_sqlite` + `add_columns` + `backfill_uuids` → result dialog with backup path + per-table counts. Same idempotency guard as the vocab-alignment entry. |

## What was tested

- **5 unit tests** in `tests/sync/test_uuid7.py` cover the generator:
  returns a `uuid.UUID` instance, version field is 7, variant is RFC
  4122 (10xx in byte 8), 1024 successive UUIDs are monotonically
  ordered, the embedded timestamp falls within ±1 ms of wall clock.
- **4 unit tests** in `tests/migrations/test_node_uuid_backfill.py`
  cover the migration: column added on each of the three tables, a
  unique index named `node_uuid` survives the run, a second
  `add_columns` run is a no-op (column count stays 1), backfill assigns
  UUID v7 (canonical 36-char form, version 7 character at index 14,
  pattern `xxxxxxxx-xxxx-7xxx-yxxx-xxxxxxxxxxxx`), and a second
  `backfill_uuids` run returns counts of zero.
- **End-to-end dry-run on the real Volterra database**: the CLI reports
  `needs ALTER TABLE + backfill (no col)` for all three target tables —
  none currently carry a `node_uuid` column. The migration is safe to
  run; user has yet to authorise `--apply`.
- **Combined sync + migrations test run**: `pytest tests/sync/
  tests/migrations/ -q` → **41 passed in 0.38 s** (29 sync + 12
  migrations). Test count breakdown: 5 vocab_types + 14
  vocab_provider_core + 2 vocab_provider_smoke + 5 i18n compat + 5
  uuid7 + 3 migration common + 5 us_vocabulary_alignment + 4
  node_uuid_backfill = 41.

## Decisions taken

- **Local UUID v7 instead of a third-party dependency.** Python's
  stdlib `uuid` module gains `uuid7()` only at 3.14 (currently
  unreleased at the time of this work); the project still has to
  support QGIS Python 3.9+. A 30-line in-repo implementation is
  cheaper than adding a `uuid7` package to `requirements.txt` and
  routing it through the runtime installer. The public API is just
  `uuid7() -> uuid.UUID` so the swap to stdlib later is a one-line
  import change.
- **Partial unique index instead of plain unique.** The index is
  declared `CREATE UNIQUE INDEX … WHERE node_uuid IS NOT NULL`. This
  matters during `--apply` recovery scenarios: if `add_columns`
  succeeded but `backfill_uuids` failed midway, the partially
  populated table has many NULL `node_uuid` rows — a plain unique
  index would reject those because a single NULL is fine but two
  NULLs are not under the SQLite default. The partial predicate
  excludes NULLs from the uniqueness check, so a re-run picks up
  where the previous one left off without dropping the index.
- **Three target tables only.** `tomba_table` is intentionally
  excluded per spec §4.5 last paragraph — burial records are out of
  scope for the bridge in Phase 1. `site_table` is also excluded
  because site-level metadata maps to a different family of s3Dgraphy
  nodes (handled at the property level, not as a typed graph node).
  These exclusions are documented in the spec and will be revisited
  if Phase 2 ingestion proves they need their own UUIDs.
- **No PostgreSQL path in Phase 1.** SQLite-only for now. The same
  logic will move to SQLAlchemy `text()` in Phase 2 when
  `GraphProjector` lands and starts consuming the `node_uuid` column
  in real reads. The CLI already takes a `--db` path so a future
  PostgreSQL DSN form (`postgresql://…`) plugs in cleanly.

## Operational notes

- **Volterra database** has none of the three target columns yet. The
  `--apply` will create all three columns, the partial unique indexes,
  and assign UUID v7 to every existing row. With ~1500 US rows and
  several hundred inventario_materiali / periodizzazione rows, the
  whole migration takes a fraction of a second.
- **Rollback strategy** is the same as Group D: the auto-backup is
  named `<db>.pre_node_uuid_backfill_<UTC>` and lives next to the
  source DB. Restoring is `--rollback <backup_path>`. Combined with
  Group D's vocab-alignment migration, a user can chain the two
  rollbacks if needed (latest backup first, reverse-chronological).

---

# Group F — `modules/s3dgraphy/` refactor (12 files)

**Date:** 7 May 2026 · **Status:** Done · **Commits:** 9
(`222ea2e0` → `349358af`)

## What was built

The 12 files under `modules/s3dgraphy/` were audited and, where
applicable, refactored to consult `VocabProvider` instead of hardcoded
EM 1.4 tables. Per-file commits keep any individual regression
selectively `git revert`-able. The disposition matrix:

| File | Disposition | Rationale |
|---|---|---|
| `cidoc_crm_mapper.py` | refactored | `CRM_CLASSES` was a hardcoded 12-key dict; replaced with a `@property` overlaying `VocabProvider.get_unit_types().cidoc_class` onto a `_LEGACY_CRM_FALLBACK`. The property still returns a `dict`, so the existing `mapper.CRM_CLASSES["stratigraphic_unit"]` lookups in 3 call sites keep working unchanged. The overlay grew the effective dictionary from 12 to 34 keys on the 0.1.40 vendor. |
| `s3dgraphy_integration.py` | refactored | Single string-literal assignment `node.node_type = 'virtual_reconstruction'` (line 113) routed through a local legacy-to-s3Dgraphy mapping that resolves to `'USVs'`. Two further occurrences flagged by Group 0 (lines 194, 606) are read paths and the `.get(...)` style respectively, so they need no change. |
| `s3dgraphy_dot_bridge.py` | refactored | The hardcoded 6-entry `color_map` for Graphviz DOT output now consults `VocabProvider.get_visual_rule()` first; legacy values are kept as fallback. |
| `matrix_graph_visualizer.py` | refactored | Multilingual `color_map` keyed by `unita_tipo` retained as fallback; primary lookup via `VocabProvider`. |
| `plotly_visualizer.py` | refactored | Same pattern as `matrix_graph_visualizer`. |
| `simple_graph_visualizer.py` | refactored | Originally listed as audit-only in the spec; the audit grep surfaced a 6-entry hardcoded `color_map`, so it was refactored alongside its peers. |
| `graphviz_visualizer.py` | refactored | Originally listed as audit-only; same finding as above. |
| `matrix_visualizer_qgis.py` | clean (audit-only) | Two string literals at lines 273/285 are QGIS rule-renderer filter expressions on the stored `node_type` data column — not a hardcoded family table. |
| `blender_integration.py` | clean (audit-only) | Per spec §7.3 — Blender export keeps its private tags. |
| `graphml_spatial_enhancer.py` | clean (audit-only) | No string-tag or hardcoded family table found. |
| `spatial_grouping_manager.py` | clean (audit-only) | No string-tag or hardcoded family table found. |
| `__init__.py` | clean | 17-line import barrel; no work. |

A single empty audit commit groups the 4 genuinely-clean files (the
`matrix_visualizer_qgis` case is included there because its data-column
filters are intentional and orthogonal to the refactor).

## What was tested

- **Existing test suite remains green**: 41 passed → 42 passed after
  Group F (one new smoke test added during the in-flight fix described
  below).
- **Python AST parse smoke** confirmed every refactored file still
  parses cleanly.
- **End-to-end smoke test** verified that on the real bundled 0.1.40
  vendor, `VocabProvider.get_visual_rule("US")` now returns the actual
  fill (`#F0F0F0`) and stroke (`#540909`) colours from the s3Dgraphy
  `node_styles[US].style` block, rather than `None` (which would have
  forced every refactored visualizer to silently fall back to its
  legacy hardcoded value).

## In-flight bug fix during Group F (commit `349358af`)

The visualizer refactors surfaced a real-shape mismatch between the
test fixtures and the actual s3Dgraphy 0.1.40 JSON files:

- The fixture `em_visual_rules_sample.json` uses a flat shape:
  `{"rules": {"<type>": {"icon", "fill", "stroke", "palette"}}}`.
- The real `ext_libs/s3dgraphy/JSON_config/em_visual_rules.json` uses
  a nested shape:
  `{"node_styles": {"<type>": {"file_2d", "style": {"fill_color", "border_color", "shape", ...}}}}`.
- Similarly, the connections datamodel uses
  `s3Dgraphy_connections_model_version` rather than the fixture's
  `s3Dgraphy_connections_version`.

`VocabProviderCore.get_visual_rule()` and `VocabProviderCore.versions`
were extended to accept BOTH shapes — the flat form for fixtures and
the nested form for the real vendor. A new smoke test
`test_real_bundled_visual_rule_for_us` asserts that, on the real
0.1.40 vendor, the visual rule for `US` returns non-empty `fill` and
`stroke` colours. Without this fix, every visualizer refactor in
Group F would have silently degraded to legacy hardcoded fallbacks
even though the JSON datamodel was being parsed correctly.

## Decisions taken

- **Per-file commits** kept the refactor `git revert`-able file by
  file. Combined with the rollback tag `pre-s3dgraphy-040`, any
  visualizer that turns out to misbehave in field testing can be
  reverted in isolation without touching the rest of Group F.
- **Legacy fallbacks preserved** in every refactored file. The new
  code path is "VocabProvider first; legacy hardcoded value if
  missing; generic neutral fallback if even that is absent." This
  keeps existing screenshots and printouts visually identical until
  s3Dgraphy ships a different palette.
- **The in-flight VocabProvider fix is intentionally permissive**: it
  accepts both the simplified fixture shape and the real nested
  shape. This means tests don't have to mirror s3Dgraphy's exact
  on-disk layout (which can evolve between versions) while the real
  vendor still drives production behaviour. Future smoke tests can
  pin specific 0.1.40 keys without breaking the fixture-based unit
  tests.

## Closing notes

Group F is the largest commit cluster in Phase 1: 9 commits, six of
which touch production source code (the three "downgraded" audit-only
files were a pleasant surprise — finding hardcoded `color_map` blocks
in `simple_graph_visualizer.py` and `graphviz_visualizer.py` means
Phase 1 cleared MORE technical debt than the spec anticipated). The
remaining work for Phase 1 is Group G: bump `metadata.txt` to
`5.1.0-alpha`, write the bilingual CHANGELOG entry, run the manual
smoke gate in QGIS, and tag the release.

---

# Group G — Release packaging (`5.1.0-alpha`)

**Date:** 7 May 2026 · **Status:** In progress — G.1/G.2/G.3 done, G.4
manual smoke gate pending user action in QGIS, G.5 tag + push pending
G.4 sign-off.

## What was done (G.1, G.2, G.3) — commit `dff6f237`

- **G.1** verified: `requirements.txt` already pins `s3dgraphy>=0.1.40`
  from Group A's commit `5988ed4a`. No change.
- **G.2**: `metadata.txt` version line bumped from `5.0.27-alpha` to
  `5.1.0-alpha`. The QGIS `changelog=` block was prepended with the
  Phase 1 entry summarising every deliverable: VocabProvider, vocab
  alignment migration, UUID v7 backfill, 12-file refactor, and the
  0.1.30→0.1.40 vendor swap.
- **G.3**: `dev_logs/CHANGELOG.md` gets a full bilingual IT/EN section
  for `5.1.0-alpha` covering all six plan deliverables and the
  file-level modifications. The section announces the rollback tag
  (`pre-s3dgraphy-040`) and the release tag the user will create after
  the smoke gate (`phase1-foundation-5.1.0-alpha`).

All 42 unit tests still pass post-bump (no regressions from version
metadata changes).

## What is pending (G.4, G.5)

### G.4 — Manual smoke gate in QGIS

User-driven; the controller cannot run QGIS. The gate is the spec's
required pre-tag check (§7.4 step 4 + plan §G.4). The user opens QGIS,
loads the Volterra project (or any other PyArchInit-managed
SQLite/PostgreSQL project), and verifies:

1. The plugin loads without exceptions in the QGIS message log.
2. Opening a US/USM record opens the form successfully.
3. The unit-type combo box (`comboBox_unita_tipo`) is populated and
   includes the EM 1.5 types (`USVs`, `USVn`, `serSU`, `WorkingUnit`,
   `NegativeStratigraphicUnit`, `TSU`).
4. Generating a Harris matrix succeeds.
5. The two new entries in the pyArchInit Maintenance menu appear:
   "Migrazioni → Allinea vocabolario US…" and "Migrazioni → Backfill
   `node_uuid`…". Optionally clicking either should open a file picker
   without raising an exception (no `--apply` is required at this
   gate; the dry-run path is enough).
6. No `ATTENZIONE`/`Error fixing view` warnings in the QGIS log
   related to s3Dgraphy or the Phase 1 refactors.

If anything fails, the user can `git checkout pre-s3dgraphy-040` to
return to the pre-Phase-1 state. The migration scripts are independent
and can be invoked manually from CLI for debugging.

### G.5 — Tag and push

Run after G.4 sign-off:

```bash
git tag -a phase1-foundation-5.1.0-alpha -m "Phase 1 (Foundation) of PyArchInit ↔ s3Dgraphy"
git push origin Stratigraph_00001
git push origin phase1-foundation-5.1.0-alpha
git push origin pre-s3dgraphy-040
```

## In-flight Phase 1 hotfix (commit `09b9fbde`)

While preparing the manual smoke gate, the user reported that the
"Extended Matrix" export produced an empty `.graphml` file alongside
the (correct) DOT and `_s3dgraphy.json` outputs. Investigation traced
the root cause to a long-standing bug in
`resources/dbfiles/dot.py::Edge.initFromString()`:

- The DOT file emitted by `S3DGraphyDotBridge` uses node IDs that
  include the site name, e.g. `"Castelseprio Casa Piccoli_1_36"`.
  These IDs are properly double-quoted in the DOT source.
- The edge parser split the edge line via
  `line.replace('->', ' ').split()` — naive whitespace split that did
  not respect double-quoted runs.
- Result: every edge ended up with `src="Castelseprio"`,
  `dest="Casa"`. Both lookups missed the (correctly-named) node
  dictionary keys, so `dottoxml.exportGraphml` wrote zero `<edge>`
  elements and, with this dataset, produced a 0-byte file.

The bug is **pre-existing** (unrelated to the Phase 1 refactor — git
blame on the affected line predates this branch). Phase 2 / AI03 was
already going to retire the entire DOT-parser code path by delegating
GraphML serialisation to s3Dgraphy's always-EM-current writer; that
remains the proper long-term fix. In the meantime this hotfix
unblocks the Phase 1 smoke gate so the user can validate the new
combo-box behaviour against a working Extended Matrix export.

The hotfix is a 36-line addition to `dot.py`: a new
`_split_respecting_quotes()` helper alongside the existing
`findUnquoted()` / `findLastUnquoted()` siblings, and a single-line
swap at the call site in `Edge.initFromString()`. Regression-tested
against four cases (bare identifiers, single-token quoted,
multi-word quoted, multi-word with attributes); on the real
Volterra/Castelseprio export the parser now extracts 655 edges with
correct full src/dest (was: 655 with truncated src/dest, all of which
fell through the node-lookup and were lost during GraphML write).

The 42-test sync + migrations suite is unaffected (the fixed code
path is in legacy DOT/GraphML production code that has no
unit-test coverage today — this gap will close in Phase 2 when the
delegation to s3Dgraphy lands and replaces the parser entirely).

## Second in-flight Phase 1 hotfix (commit `d0dcd828`)

The first hotfix patched `resources/dbfiles/dot.py`, but the user
reported that the plugin loads `dot.py` from `~/pyarchinit/bin/`
rather than directly from the plugin tree. Investigation surfaced
the deployment mechanism:

- `pyarchinit_Folder_installation.install_dir()` copies a list of
  utility files (`dot.py`, `dottoxml.py`, `X11Colors.py`,
  `EM_palette.graphml`, etc.) from `resources/dbfiles/` into
  `~/pyarchinit/bin/` at first install.
- This routine runs **only** when `~/pyarchinit/` does not exist
  (`__init__.py:805`: `if not os.path.exists(PYARCHINIT_HOME):
  fi.install_dir()`). On every subsequent QGIS reload the
  install path is skipped and the runtime copies stay frozen at
  whatever version shipped on the day of first install.
- `Pyarchinit_OS_Utility.copy_file()` is itself a no-op when the
  destination already exists (`if os.access(destination, 0):
  return 0`), so even if the install routine were re-run it
  wouldn't refresh anything.

Net effect: a bug fix in `resources/dbfiles/dot.py` never reaches
the actual `~/pyarchinit/bin/dot.py` that the plugin imports at
runtime. Users have a stale copy and keep hitting the original bug.

The fix introduces an auto-refresh path:

- A new class constant `MAINTENANCE_BIN_FILES` on
  `pyarchinit_Folder_installation` lists the four files the plugin
  owns end-to-end (`dot.py`, `dottoxml.py`, `X11Colors.py`,
  `EM_palette.graphml`). User-editable assets (`epoche_storiche.csv`,
  `thesaurus_template.csv`, `template_report_adarte.docx`) are
  deliberately excluded so their first-install copy is preserved.
- A new method `install_or_update_maintenance_files()` compares
  mtime + size between the bundled source under `resources/dbfiles/`
  and the installed copy under `~/pyarchinit/bin/`, overwriting
  via `shutil.copy` (force-overwrite) when src is strictly newer
  or sizes differ. Idempotent on re-runs.
- `__init__.py` calls the method on every plugin reload (in the
  branch where `~/pyarchinit/` already exists), wrapped in
  try/except so a refresh failure never blocks plugin startup.
- The same method is also called at the end of `installConfigFile()`
  so first installs go through the same code path.

End-to-end tested locally: a stale 1000-byte dot.py was refreshed
to the bundled 49920-byte version on first call; the second call
was a no-op (mtime + size match). The user's real
`~/pyarchinit/bin/dot.py` was also refreshed in the same run, so
the Extended Matrix smoke test can proceed without a manual delete
of the old file.

## Phase 1 known limitations carried over to Phase 2 / AI03

During Group G smoke preparation the user surfaced four Extended
Matrix shortcomings beyond the parser hotfix. After investigation
all four are pre-existing limitations of the legacy DOT → GraphML
pipeline (`s3dgraphy_dot_bridge.S3DGraphyDotBridge.s3dgraphy_to_dot`
+ `_convert_dot_to_graphml` + `dottoxml.exportGraphml`) and are
**out of scope for Phase 1 finalization**. They are tracked here
as the input for the Phase 2 plan covering action item AI03.

| # | Issue | Root cause | Phase 2 / AI03 fix |
|---|---|---|---|
| L1 | The `Configure spatial/functional groupings` checkbox produces an empty `.graphml` | `apply_grouping_to_dot` rewrites the DOT in a way that drops nodes not assigned to any group; `enhance_graphml_with_groups` then truncates the file when its yfiles-namespace assumptions don't match what `dottoxml.exportGraphml` produced | s3dgraphy `GraphMLExporter` builds the swimlane structure natively from `EpochNode` instances with a proper TableNode container, so post-hoc grouping is unnecessary |
| L2 | The `.graphml` is not subdivided in rows by period | `s3dgraphy_to_dot` emits a flat DOT with `rankdir=TB` but no `subgraph cluster_…` per `periodizzazione_table` row, no `rank=same` per phase | s3dgraphy `EpochSwimlanesGenerator` (in `exporter/graphml/epoch_generator.py`) calculates per-epoch positions and emits a TableNode swimlane with one row per `EpochNode` |
| L3 | Edge styles only cover 2 of the 36 EM 1.5 edge types (only `has_same_time` → dashed, `generic_connection` → dotted) | The DOT generator at `s3dgraphy_dot_bridge.py:130-134` hardcodes a 2-entry mapping and treats every other relation as "no style" | s3dgraphy `EdgeGenerator` (in `exporter/graphml/edge_generator.py`) consults the full vocabulary from `s3Dgraphy_connections_datamodel.json` and writes the correct yEd line style per edge type |
| L4 | No transitive reduction (Harris Matrix redundancies are kept verbatim) | The pipeline never invokes `tred` or any equivalent | s3dgraphy `TemporalInferenceEngine.transitive_reduction()` runs at `graphml_exporter.py:111-119` as a built-in step before XML emission |

All four are addressed in one move by **delegating GraphML
serialisation to `s3dgraphy.exporter.graphml.GraphMLExporter`**.
This is the AI03 deliverable already in the spec and was always
scheduled for Phase 2; the Phase 1 release ships with the legacy
pipeline carrying the limitations above so that the foundation
work (VocabProvider, vocab alignment, UUID v7 backfill, 12-file
refactor) can ship cleanly without expanding scope.

**Workaround for users who need Extended Matrix output now:** the
`_s3dgraphy.json` companion file already produced by the green
"Extended Matrix" button is complete (~300 KB on a Volterra-size
dataset) and contains the full graph with every edge type
correctly typed. Any tool that consumes s3dgraphy native JSON can
read it directly; the legacy `.graphml` should be treated as a
preview rather than the canonical output until AI03 lands.

### Phase 2 entry criteria (informational)

The next plan to write is `docs/superpowers/plans/2026-MM-DD-phase-2-graphml-delegation.md`,
which will cover:

- AI03: drop the in-house GraphML writer and delegate to
  `s3dgraphy.GraphMLExporter`. Required preparation: refactor
  `S3DGraphyIntegration.import_from_pyarchinit()` to produce
  typed `StratigraphicUnit`/`StratigraphicUnitMasonry`/…
  instances (not generic `Node`); convert
  `periodizzazione_table` rows to `EpochNode` with
  `has_first_epoch` edges; expand the relationship-mapping table
  to cover the full s3dgraphy edge vocabulary
  (`overlies`, `cuts`, `fills`, `abuts`, `is_bonded_to`, …) per
  the JSON datamodel.
- AI04: the bidirectional bridge proper —
  `populate_graph()` and `populate_list()` macro-functions per
  Reference Document v0.1 §6.
- AI05: `ParadataStore` for `paradata.graphml` per project,
  the editable layer for authors / licenses / virtual nodes.

Estimated effort: 1–2 weeks per the spec's §11 phasing.

---

# Phase 1 closure — manual smoke gate (G.4) + tag (G.5)

**Date:** 7 May 2026 · **Status:** **Phase 1 complete and tagged.**

## G.4 manual smoke gate result

The user ran the gate in QGIS on `pyarchinitcs_SCHEDE2024.sqlite`
(Castelseprio Casa Piccoli) and reported **`✅ smoke OK`** covering
every checklist item:

1. Plugin loaded without exceptions in the QGIS Log Messages panel.
2. The US/USM unit-type combo box exposes the EM 1.5 vocabulary
   (`USVs`, `USVn`, `serSU`, `UL`, `USN`, `TSU`, …) sourced from
   the s3Dgraphy 0.1.40 JSON datamodel via `VocabProvider`. Legacy
   `USVA`/`USVB`/`USVC` are no longer in the picker.
3. The green "Extended Matrix" button (without spatial-grouping
   flag) produces a populated `.graphml` (655 edges + 298 nodes
   on the Volterra dataset) in addition to the DOT and the two
   JSON companions.
4. The classic Harris-matrix generator works.
5. Both new `Maintenance → Migrazioni` menu entries appear and
   open a file picker without exception. The vocab-alignment
   migration's dry-run on the Volterra DB reports zero rows
   (no `USVA`/`USVB`/`USVC` present) — `--apply` was deliberately
   not run.

The 4 documented Extended Matrix limitations (no spatial-grouping
support, no period swimlanes, partial edge styling, no transitive
reduction) are out of scope per Option A and remain on the Phase 2
backlog as described above.

## G.5 — Release tag + push

Created locally and pushed to `origin/Stratigraph_00001`:

| Tag | Points at | Purpose |
|---|---|---|
| `phase1-foundation-5.1.0-alpha` | `8fceff79` | Phase 1 release marker |
| `pre-s3dgraphy-040` | `cf11f027` | Pre-Phase-1 rollback point |

Branch `Stratigraph_00001` advanced from `cf11f027` (the rollback
point recorded at Group 0) to `8fceff79`, picking up 31 new commits
across the eight Groups plus the two in-flight hotfixes plus the
release-packaging chore.

The push was clean. GitHub returned its standing dependabot
advisory for the repository (19 historical vulnerabilities on the
default branch) which is unrelated to this branch's content and
predates Phase 1.

## Closing summary

Phase 1 (Foundation) of T5.4 is closed. The deliverables match
the implementation plan one-for-one:

- s3Dgraphy 0.1.30 → 0.1.40 vendored, version pinned in
  `requirements.txt`.
- New `modules/s3dgraphy/sync/` package (5 files) ships
  `VocabProvider` + `VocabProviderCore` + `Family`/`UnitType`/
  `EdgeType`/`VisualRule`/`ParadataType`/`VocabularyVersion`
  dataclasses + a local UUID v7 generator. Public API frozen
  for Phase 1.
- `modules/utility/pyarchinit_i18n_stratigraphic.py` is now an
  adapter over `VocabProvider` while preserving its public
  surface for one release. The unit-type picker exposes 27
  items (was 14).
- Two one-shot SQL migrations under `scripts/migrations/`,
  both `--dry-run / --apply / --rollback` with auto-backup,
  both wired into a new `Maintenance → Migrazioni` menu in the
  QGIS plugin.
- 12 files under `modules/s3dgraphy/` audited; 7 refactored to
  consult `VocabProvider`; 4 verified clean.
- 42 unit tests passing in `tests/sync/` + `tests/migrations/`.

Reference contracts (committed):

- Spec: `docs/superpowers/specs/2026-05-04-s3dgraphy-bidirectional-sync-design.md`
  (aligned with both the meeting minutes and Reference Document v0.1).
- Implementation plan: `docs/superpowers/plans/2026-05-06-phase-1-foundation.md`
  (8 Groups, 37 tasks, ~150 steps; executed via the Subagent-Driven
  Development workflow with two-stage review on the foundation pair).
- This dev log: `docs/superpowers/dev-log/T5.4_PyArchInit_Dev_Log.{md,docx}`
  (rolling English-language partner-facing summary, kept in step with
  every Group commit).

Next step is the Phase 2 plan covering AI03 (delegate GraphML to
s3Dgraphy `GraphMLExporter`), AI04 (bidirectional bridge —
`populate_graph` / `populate_list`), and AI05 (`ParadataStore`).
The four Extended Matrix limitations carried over here will be
the natural validation gate for AI03.

# Phase 2 — AI03 closed (GraphML delegation)

**Date:** 7 May 2026 · **Status:** **Done.** · **Commits:** approx
`8df46836..HEAD` (the pre-AI03 documentation commit was `8df46836`;
the AI03 implementation opens at `19924971` with the `ExportResult`
dataclass and closes with this group's metadata bump and changelog).

This phase delivered AI03 of the T5.4 backlog: the GraphML branch of
the "Extended Matrix" export now delegates to s3Dgraphy's
`PyArchInitImporter` + `GraphMLExporter` instead of the legacy
DOT→GraphML pipeline. The cut-over is clean: the legacy code path is
deleted, not gated behind a fallback flag. The four Extended Matrix
limitations carried over from Phase 1 (empty graphml when the
spatial-grouping flag was set, no period swimlanes, partial edge
styling, no transitive reduction) close in a single move, because
the s3Dgraphy exporter handles all four natively.

## What was built

A new orchestration module, `modules/s3dgraphy/sync/graphml_writer.py`,
hosts three pure helpers — `build_pyarchinit_mapping()`,
`count_epoch_nodes()`, `count_topological_edges()` — and a single
public entry point `write_graphml_via_s3dgraphy(sqlite_path,
output_path) -> ExportResult`. The orchestrator opens the SQLite via
SQLAlchemy, constructs `PyArchInitImporter(connection_url=...,
mapping_name="pyarchinit_us_mapping", overwrite=True)`, populates
the in-memory graph, and then runs an internal helper
`_enrich_pyarchinit_graph()` which adds the artefacts that the
importer does not produce on its own: `EpochNode`s built from
`periodizzazione_table`, `has_first_epoch` edges from each US to its
period, and topological `is_before` edges parsed from
`us_table.rapporti`. With the graph fully decorated, the orchestrator
calls `GraphMLExporter(graph, output_path).export()` (which performs
its own transitive reduction) and returns an `ExportResult` carrying
`node_count`, `edge_count`, `epoch_count`, `tred_removed_edges` and
the optional warnings list. `tred_removed_edges` is computed
arithmetically as `pre_edges - post_edges` rather than parsed from
the GraphML output: this avoids a fragile regex over the exporter's
implementation details. The mapping name is `pyarchinit_us_mapping`
(the implementation plan's earlier `pyarchinit` was wrong — it does
not match the JSON file shipped with s3Dgraphy 0.1.40).

The bridge cut-over was performed in five per-file commits so any
single step is easy to revert. `s3dgraphy_dot_bridge.py` had its
`_convert_dot_to_graphml()` private function and its `dottoxml`
import removed, and its dispatcher now calls the new orchestrator
when the requested format is GraphML; the DOT branch is left
untouched because Harris-matrix exporters still rely on it.
`graphml_spatial_enhancer.py` was deleted in full — it was a
post-processor that nested nodes inside `y:ProxyAutoBoundsNode`
subgraphs after the GraphML had already been written, and
`GraphMLExporter` emits those swimlanes natively. `spatial_grouping_manager.py`
lost both its `apply_grouping_to_dot()` function and the
`SpatialGroupingDialog` Qt class (the latter was tied to the old DOT
pipeline); the module now keeps only the grouping data structures
that are still consulted elsewhere. `S3DGraphyExportDialog` lost the
`cb_spatial_grouping` checkbox plus all the code that read it, since
spatial grouping is now driven by the s3Dgraphy exporter without a
user toggle. Finally, `resources/dbfiles/dot.py` and `dottoxml.py`
were intentionally left in place: the legacy Harris-matrix exporters
still consume them and they are out of AI03 scope (revisit in AI04).

The `pyarchinit_db_manager` module gained a new `get_sqlite_path()`
method that returns the absolute path of the SpatiaLite file when
the active backend is SQLite, and `None` otherwise (PostgreSQL,
unconfigured manager). The bridge consults this signal before
invoking the orchestrator and skips the GraphML branch with an Info
dialog ("PostgreSQL backend will be supported in AI04") when the
project is wired to PostgreSQL/PostGIS. This is the agreed graceful
degradation for AI03 — `PyArchInitImporter` accepts only a SQLite
`connection_url` today, and AI04 will widen its surface.

The export dialog summary was rewritten to surface the per-format
metrics returned by the new orchestrator. After every Extended
Matrix export, the user now sees, for each emitted format
(GraphML/DOT/JSON), the node, edge and epoch counts, the number of
transitive edges removed, and any warnings (e.g. a notice when
`periodizzazione_table` is empty and `epoch_count` falls back to
zero). This replaces the pre-AI03 binary "Export completed" message.

## What was tested

The test suite under `tests/sync/` grew by 16 tests (total: 59
passing). Three of them are unit tests on the pure helpers
(`build_pyarchinit_mapping`, `count_epoch_nodes`,
`count_topological_edges`). Four exercise the end-to-end orchestrator
against a deterministic fixture, `tests/sync/fixtures/mini_volterra.sqlite`,
and are mapped one-to-one to the four Extended Matrix limitations:
the GraphML output is non-empty when grouping is active, period
swimlanes are present in the output, `is_before` edges carry their
intended y:Edge styling, and `tred_removed_edges` is greater than
zero on a graph that contains a redundant transitive edge. Four more
tests cover `db_manager.get_sqlite_path()` (valid SQLite path,
missing SQLite path, PostgreSQL → `None`, uninitialized manager →
`None`), and five pre-existing exception-helper tests round out the
file.

The fixture itself, `mini_volterra.sqlite`, contains six US records
with cross-cutting `rapporti` (so the transitive-reduction case is
non-trivial), three rows in `periodizzazione_table`, and a
simplified geometry layer. It is committed as a binary blob so the
suite runs reproducibly without depending on production DBs. The
`_enrich_pyarchinit_graph()` helper inside the orchestrator is the
deliberate workaround for a Phase-1 finding: `PyArchInitImporter`
alone does not emit `EpochNode`s, `has_first_epoch` edges or
topological `is_before` edges. The orchestrator therefore enriches
the graph before handing it to the exporter; the four pipeline tests
all pass because of this enrichment, and the workaround is
documented in-line in the module so the next maintainer knows why
it exists.

## Decisions taken

The cut-over is **clean — there is no fallback flag**. The legacy
DOT→GraphML pipeline was a maintenance liability (it produced an
empty file under the spatial-grouping flag and could not emit
swimlanes); shipping it side-by-side with the s3Dgraphy delegation
would have doubled the surface area without buying any user value.
Rolling back is, instead, a `git revert` of the five per-file Group E
commits.

`tred_removed_edges` is computed arithmetically (`pre_edges -
post_edges`) rather than parsed from the GraphML output via regex.
The arithmetic version is robust to formatting changes inside
`GraphMLExporter` and is symmetrical to the way `node_count`/`edge_count`
are produced.

The mapping name passed to `PyArchInitImporter` is
`pyarchinit_us_mapping`, not `pyarchinit` as the early plan draft
suggested. The vendored s3Dgraphy 0.1.40 ships
`pyarchinit_us_mapping.json` under `ext_libs/s3dgraphy/`, and this
is the only mapping name the importer recognises out of the box.
This was caught while wiring up the first pipeline test and is
documented in the orchestrator docstring.

## Operational note

Developers pulling this branch must re-run
`python scripts/modules_installer.py` only if their
`ext_libs/s3dgraphy/` is older than 0.1.40; otherwise no extra step
is needed because Phase 1 already vendored 0.1.40. The Phase 2
metadata bump is documentation-only and adds no new third-party
dependencies. The Phase 2 manual smoke gate (G.4) and tag (G.5) are
the same shape as Phase 1 and will be performed next.

---

# Phase 2 — AI04 Bridge bidirezionale

**Date:** 2026-05-08 → 2026-05-08
**Tag:** `phase2-ai04-bridge-bidirectional-5.3.0-alpha`
**Spec:** `docs/superpowers/specs/2026-05-08-ai04-bridge-bidirectional-design.md`
**Plan:** `docs/superpowers/plans/2026-05-08-ai04-bridge-bidirectional.md`

## What was built

Library bridge between PyArchInit SQL tables and the s3dgraphy
in-memory `Graph` model. New public surface in
`modules/s3dgraphy/sync/`:

- `GraphProjector.populate_graph(db_path, sito) -> Graph` — Strategy
  D thin wrapper around `_enrich_pyarchinit_graph` (promotion to a
  proper class is queued for AI05). The wrapper invokes
  `PyArchInitImporter` for the base node set, then enriches with
  epochs, has_first_epoch edges, rapporti edges, and propagates
  the 12 mapped column values into node attributes.
- `GraphIngestor.populate_list(graph, db_path, sito, *, dry_run,
  create_missing_epochs) -> IngestResult` — atomic transaction,
  UPDATE selettivo on the 12 mapped columns, full exception
  hierarchy (`SchemaMismatchError`, `UnknownUnitaTipoError`,
  `SiteMismatchError`, `MissingEpochError`).
- `ConflictResolver` stub returning `ConflictResolution.GRAPH_WINS`
  — interface in place for AI06+ pluggable strategies.
- `IngestResult` + `ConflictRecord` frozen dataclasses.
- CLI `scripts/s3dgraphy_sync.py` with `export`/`import` subcommands;
  `import` defaults to dry-run, `--apply` required for writes.
- `S3DGraphyExportDialog` gets a QTabWidget with the existing Export
  tab (untouched) plus a new Import tab.

## Acceptance criteria status

All 15 AC pass. AC-2 byte-identical baseline (now structural
fingerprint, see commit `878f87a3`) stayed green at every commit
during AI04 — the regression guard for AI03.

## Test counts at Group boundaries

- Baseline (post-AI03): 60 passing
- After Group A (dataclasses + stub + exceptions): 71
- After Group B (GraphProjector wrapper): 76
- After Group C (populate_list dry-run): 84
- After Group D (populate_list write mode): 86
- After Group E (round-trip + idempotent): 88
- After Group F (CLI helper + subprocess tests): 92
- After Group G (UI Import tab): 92 (no new tests)
- Final: **92 passed** in <2s, no QGIS dependency

## Manual smoke gate (G in plan)

User-driven validation in QGIS — see plan Group H Task H.4. The
controller waits for the user to confirm before tagging.

## Carry-overs

- `paradata.graphml` per project — AI05.
- Pluggable `ConflictResolver` strategies — AI06.
- `failure_mode={atomic, best_effort, by_group}` — AI06+.
- Promotion of `GraphProjector` from wrap (D) to real class (A)
  — AI05.
- s3dgraphy GraphML round-trip preserves PyArchInit-specific node
  attributes (currently lossy on parse) — AI06+.

## Notes & deviations from plan

1. The byte-identical AC-2 baseline turned out to be order-flaky
   because s3dgraphy 0.1.40's GraphMLExporter assigns physical node
   IDs (`n0::nN`) in iteration order. Replaced with a structural
   fingerprint (commit `878f87a3` in Group 0). Same regression
   guarantee, deterministic outcome.
2. Group B's GraphProjector grew from a "thin wrapper" (~80 lines as
   per the plan) to ~244 lines once it had to also call
   `PyArchInitImporter` and propagate the MAPPED_COLUMNS into node
   attributes — without those, downstream tests in Group C onwards
   would have been vacuously green. The spec §3.2 explicitly calls
   for the importer reuse, so this is consistent with the spec.
3. The CLI `import --apply` write path is lossy on the GraphML
   round-trip because s3dgraphy's GraphMLImporter strips
   PyArchInit-specific attributes on parse. The idempotent invariant
   D4-C accepts this (it asserts convergence, not full preservation).
   Strengthening the round-trip is a follow-up for AI06+.

---

# Phase 2 — AI05 ParadataStore + UI authoring + Strategy A + edge styling

**Date:** 2026-05-08 → 2026-05-08
**Tag:** `phase2-ai05-paradata-store-5.4.0-alpha`
**Spec:** `docs/superpowers/specs/2026-05-08-ai05-paradata-store-design.md`
**Plan:** `docs/superpowers/plans/2026-05-08-ai05-paradata-store.md`

## What was built

Phase 2 closure. Four scope items:

1. **ParadataStore** — atomic-safe CRUD for `paradata_{sito}.graphml`
   hosting AuthorNode/LicenseNode/EmbargoNode (the 3 node types
   without an SQL counterpart in pyarchinit). Site-scoped file at
   `{db_path.parent}/paradata_{sito_slug}.graphml`, written via
   `.tmp` + `os.replace()` for crash safety. Low-level (read/write/
   add_node/remove_node/find) + high-level (add_author/license/
   embargo + list_*) API surface.

2. **GraphProjector Strategy A promotion** — full move of
   `_enrich_pyarchinit_graph` body into `GraphProjector._enrich_into`
   private method. Standalone function in `graphml_writer.py`
   DELETED. Single existing caller (`export_graphml()`) updated to
   use `GraphProjector().populate_graph(..., include_paradata=False)`.
   Pays the AI04 carry-over from `project_ai04_projector_refactor_plan.md`
   memory note.

3. **`include_paradata` integration** — `populate_graph(db, sito, *,
   include_paradata=True)` defaults to merge SQL + paradata. Opt-out
   via `include_paradata=False` for AI04-style strat-only output.
   Corrupt paradata file → log warning + fall back to strat-only
   (non-fatal).

4. **EdgeRegistry** — `modules/s3dgraphy/sync/edge_registry.py` with
   `resolve_edge_style(edge_type)` and `is_paradata_edge(edge_type)`.
   Reads from `s3Dgraphy_connections_datamodel.json` and
   `em_visual_rules.json` (s3dgraphy 0.1.40). Override-wins via
   `_PYARCHINIT_EDGE_OVERRIDES` (currently empty). Wired into the
   `graphml_writer.py` post-processor as the canonical paradata
   classification source.

## UI

New `gui/dialog_paradata_manager.py` — single QDialog with 3-tab
QTabWidget (Authors / Licenses / Embargoes). Each tab has a
QTableWidget showing existing entries + form for adding new ones +
Remove-selected button. Triggered by a "Manage paradata" button
added to the US/USM scheda next to the existing green Extended
Matrix button.

## CLI

`scripts/s3dgraphy_sync.py paradata` subcommand with 7
sub-subcommands: add-author, list-authors, add-license,
list-licenses, add-embargo, list-embargos, remove. Same exit-code
contract as AI04 CLI (0 success / 1 GraphSyncError / 2 argparse).

## Test counts

- Baseline (post-AI04+AI04.1): 94 passing
- Post-Group A (edge_registry): 99 passing
- Post-Group B (paradata_store): 116 passing
- Post-Group C (Strategy A): 123 passing
- Post-Group D (fixture): 123 passing (no new tests)
- Post-Group E (round-trip + idempotent): 126 passing
- Post-Group F (CLI): 132 passing
- Post-Group G (UI): 132 passing + 1 skipped (Qt not in CI)

Final: ≥120 target hit (132 passed, 1 skipped).

## Acceptance criteria

All 18 ACs from spec §7 satisfied. AC-2 byte-identical baseline
stayed green at every commit during Group C (Strategy A's
highest-risk move). AI04 round-trip + idempotent + CLI helper
tests stayed green throughout.

## Manual smoke gate (Group H.4)

User-driven validation in QGIS — see plan Group H Task H.4.

## Carry-overs (deferred to AI06+)

- Per-node paradata edges (Author → has_authored → US)
- Pluggable ConflictResolver strategies + UI dialog
- Configurable failure_mode={atomic, best_effort, by_group}
- ORCID/SPDX/date validation
- File locking for concurrent writes
- VirtualSpecialFindUnit authoring layer

## Phase 3+ scope (unchanged)

- SyncEngine + DatacenterClient + REST API (Phase 3)
- GraphDBBackend + SPARQL (Phase 4)
